using System;

namespace SAL.Flatbed
{
	/// <summary>Plugin interface that provides a wrapper for group of other plugins</summary>
	/// <remarks>Transferred from Interface.Windows because of basis is used on Windows &amp; Web applications</remarks>
	public interface IPluginKernel : IPlugin
	{
	}
}